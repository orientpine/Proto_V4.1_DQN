# 사용법

# cd sma_swarm(setup.py가 있는 directory)
# pip install -e .

from setuptools import setup

setup(name='larva_ver4',version='0.0.5',install_requires=['gym','pybullet'])
#setup(name='sma_swarm',version='0.0.1',install_requires=['gym','pybullet','baselines'])